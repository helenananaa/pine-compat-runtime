//@version=5
strategy("session overnight filled orders", pyramiding=2)
strategy.risk.max_intraday_filled_orders(2)
if bar_index == 0
    strategy.entry("A", strategy.long, qty=1)
if bar_index == 1
    strategy.entry("B", strategy.long, qty=1)
plot(strategy.position_size)

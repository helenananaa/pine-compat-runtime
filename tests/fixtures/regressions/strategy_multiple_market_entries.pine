//@version=6
strategy("multiple market entries", pyramiding=3)
if bar_index == 0
    strategy.entry("A", strategy.long, qty=1)
    strategy.entry("B", strategy.long, qty=1)
    strategy.entry("C", strategy.long, qty=1, limit=5)

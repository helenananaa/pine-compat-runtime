//@version=6
strategy("close same id allocations", pyramiding=2)
if bar_index == 0
    strategy.entry("L", strategy.long, qty=1)
if bar_index == 1
    strategy.entry("L", strategy.long, qty=1)
if bar_index == 2
    strategy.close("L")

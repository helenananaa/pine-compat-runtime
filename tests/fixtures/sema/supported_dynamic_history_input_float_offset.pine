//@version=5
indicator("Supported dynamic history input float offset")
length = input.int(2, "Length")
lag = length / 2
zxLag = length / 2 == math.round(length / 2) ? length / 2 : (length - 1) / 2
plot(close[lag])
plot(close[zxLag])

//@version=6
strategy("Strategy order default quantity short reduces long")
if bar_index == 0
    strategy.entry("L", strategy.long, qty=1)
if bar_index == 1
    strategy.order("X", strategy.short)
plot(strategy.position_size)

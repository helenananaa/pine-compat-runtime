//@version=6
strategy("Benchmark trend", initial_capital=1000000)
fast = ta.sma(close, 5)
slow = ta.sma(close, 20)
if ta.crossover(fast, slow)
    strategy.entry("L", strategy.long, qty=1)
if ta.crossunder(fast, slow)
    strategy.close("L")
plot(fast)
plot(slow)
